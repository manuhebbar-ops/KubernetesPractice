apiVersion: v1
kind: Pod
metadata:
  name: labelcont
  labels:
    day: friday
    class: kubarnetes    

spec:
  containers:
    - name: resource
      image: centos
      command: ["/bin/bash","-c","while true; do echo hello-manu; sleep 5; done"]

